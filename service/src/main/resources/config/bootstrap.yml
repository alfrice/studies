spring:
  application:
    name: AlicesLoveChild
  cloud:
    config:
      enabled: false
    discovery:
      enabled: true

eureka:
  client:
    enabled: true
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: ${EUREKA_ZONE}